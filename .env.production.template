# Production/staging Vercel deployments must use same-origin relative paths, proxied server-side
# to the backend via vercel.json rewrites — never the backend's own hostname here. See
# .env.production and vercel.json.
VITE_API_URL=/api
VITE_SIGNALR_URL=/hubs
