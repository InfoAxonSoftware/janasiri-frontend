import UnifiedSupportPage from '../../components/support/UnifiedSupportPage';
import { repSupportConfig } from '../support/roleSupportConfigs';

export default function RepSupport() {
  return <UnifiedSupportPage config={repSupportConfig} />;
}
